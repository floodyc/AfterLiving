{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @legacyvideo/db build && pnpm --filter @legacyvideo/shared build && pnpm --filter @legacyvideo/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @legacyvideo/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
